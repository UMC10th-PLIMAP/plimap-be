# Prod 환경 설정
# SECURITY: 이 파일은 Git에 추적됩니다. 실제 비밀값을 직접 작성하지 말고 Secret Manager 환경변수만 참조합니다.

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 8
      minimum-idle: 0
      connection-timeout: 5000
  flyway:
    user: ${FLYWAY_USERNAME}
    password: ${FLYWAY_PASSWORD}
  data:
    redis:
      url: ${REDIS_URL}
  security:
    oauth2:
      client:
        registration:
          kakao:
            redirect-uri: ${KAKAO_REDIRECT_URI}
          google:
            redirect-uri: ${GOOGLE_REDIRECT_URI}

oauth:
  redirect-uri: ${OAUTH_REDIRECT_URI}
  allowed-frontend-origins: ${OAUTH_ALLOWED_FRONTEND_ORIGINS}

profile-image:
  storage:
    gcs:
      bucket: ${PROFILE_IMAGE_BUCKET}
      public-base-url: ${PROFILE_IMAGE_PUBLIC_BASE_URL:https://storage.googleapis.com}
      cache-control-seconds: 31536000

springdoc:
  api-docs:
    enabled: ${SWAGGER_ENABLED:false}
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:false}

logging:
  structured:
    format:
      console: logstash
    json:
      rename:
        level: severity
        '@timestamp': time
      add:
        environment: prod
